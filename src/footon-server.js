/*
 * footon - simple file-system based JSON store with querying, built for packaged node-webkit applications
 * author: gordon hall <gordon@gordonwritescode.com>
 * 
 * footon-server is for connecting to a remote footon app
 */

// expose connection listener function
// this will allow on to set up a footon
// server with one function call or from
// the command line using footon CLI
module.exports.listen = function(port) {
	
};

// expose connection listener function
module.exports.connect = function(options) {
	// options.host
	// options.port
	// options.database
};
